# Contact form delivery. Without these the form still validates and succeeds,
# and the submission is logged to the server console instead of emailed.
RESEND_API_KEY=
CONTACT_TO_EMAIL=hello@zulaiz.com
# Must be a domain you have verified in Resend.
CONTACT_FROM_EMAIL="Zulaiz site <hello@zulaiz.com>"
